<?xml version="1.0" encoding="UTF-8"?>
<job xmlns="urn:proactive:jobdescriptor:dev" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="urn:proactive:jobdescriptor:dev ../../src/scheduler/src/org/ow2/proactive/scheduler/common/xml/schemas/jobdescriptor/dev/schedulerjob.xsd" name="slurm-job-submit" priority="normal" cancelJobOnError="true">

    <variables>
        <variable name="sbatch_workdir" value="/tmp/"/>
        <variable name="sbatch_arguments" value="--exclusive"/>
        <variable name="sbatch_script" value="USERSPACE:/test.sh"/>
        <variable name="sbatch_script_arguments" value=""/>
    </variables>

	<taskFlow>

        <task name="submit-job">
            <inputFiles>
                <!-- Only script -->
                <!--<files includes="${sbatch_script}" accessMode="transferFromUserSpace"/>-->
                <!-- All files -->
                <files includes="*" accessMode="transferFromUserSpace"/>
            </inputFiles>
            <scriptExecutable>
                <script >
                    <code language="groovy">
                        <![CDATA[
                        import groovy.json.JsonOutput
                        import org.ow2.proactive.datascale.slurm.OutputParser

                        def sbatch = "sbatch"
                        def sbatch_workdir = "${sbatch_workdir}"
                        def sbatch_arguments = "${sbatch_arguments}"
                        def sbatch_script = "${sbatch_script}"
                        def sbatch_script_arguments = "${sbatch_script_arguments}"

                        def root = new File(new URI(localspace.getRealURI())).getAbsoluteFile()

                        def workdir = new File(sbatch_workdir).getAbsoluteFile()
                        if (!workdir.isDirectory()) {
                            println "Not a directory: '$workdir'. Using home directory instead."
                            workdir = new File(System.getProperty("user.home")).getAbsoluteFile()
                        }

                        sbatch_script = sbatch_script.replace("USERSPACE:", root.getAbsolutePath())
                        sbatch_script_arguments = sbatch_script_arguments.replace("USERSPACE:", root.getAbsolutePath())

                        def cmd = sbatch + " " + " " + sbatch_arguments + " " + sbatch_script + " " + sbatch_script_arguments
                        def p = cmd.execute([], workdir)
                        p.waitFor()
                        def stderr = p.err.text
                        def stdout = p.text
                        def errcode = p.exitValue()

                        def job = getJobId(stdout)

                        result = JsonOutput.toJson(
                                [
                                        job: job,
                                        errcode: errcode,
                                        cmd: cmd,
                                        workdir: workdir.getAbsolutePath(),
                                        script: sbatch_script,
                                        stdout: stdout,
                                        stderr: stderr
                                ])

                        println "Result: " + JsonOutput.prettyPrint(result)

                        def getJobId(txt) {
                            def regx = /(?s).*?job (\d+).*?/
                            def matcher = ( txt =~ regx )

                            if (matcher.matches()) {
                                return matcher[0][1]
                            } else {
                                return "unknown"
                            }
                        }

                        ]]>
                    </code>
                </script>
            </scriptExecutable>
        </task>

    </taskFlow>
</job>

